using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HullAnimatorBase : MonoBehaviour {
	//TODO: this is currently an empty class because hulls don't currently have any function that
	//need to be called, but this may change if, for example, an indicator light needs to be toggled
	//or something of similar effect
}
